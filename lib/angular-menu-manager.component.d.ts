import { AfterViewInit, ElementRef } from '@angular/core';
export declare class AngularMenuManagerComponent implements AfterViewInit {
    menuId: any;
    apiUrl?: any;
    private el;
    constructor(el: ElementRef);
    ngAfterViewInit(): void;
}
